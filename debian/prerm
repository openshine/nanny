#!/bin/sh -e

# Do not restart mobile-manager on upgrades, only stop if we remove the package
if [ "$1" = "upgrade" ]; then
    exit 0
fi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0

